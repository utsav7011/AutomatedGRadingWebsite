create the virtual environment for server in flask.

start the venv 
.\venv\Scripts\activate

Install all the dependencies of flask

Install all the depencies of the react.

Start the server
flask --app server run

Start the ract app:
npm start